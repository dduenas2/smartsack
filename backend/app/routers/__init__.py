"""
Routers de la API REST de SmartSack.

Cada archivo en este paquete agrupa los endpoints de un dominio:
auth, machines, orders, events, dashboard, predictions y chat.
Se montarán en `app.main` con `app.include_router(...)`.
"""
