{
  "name": "smartsack-e2e",
  "private": true,
  "version": "0.1.0",
  "description": "Pruebas end-to-end de SmartSack con Playwright. Proyecto independiente del SPA porque el node_modules del frontend lo gestiona Docker (usuario root); aquí corremos en el host contra el stack levantado en http://localhost.",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0"
  }
}
